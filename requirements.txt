# ============================================================
# 致远 OA 公文 Agent — 核心依赖（锁定兼容版本）
# ============================================================
# 更新方式：pip install -r requirements.txt
# 锁版本方式：pip freeze | Select-String "关键包名" >> requirements.lock.txt

# --- LangChain 全家桶 ---
langchain>=0.3.0,<2.0.0              # 核心框架
langchain-openai>=0.2.0,<2.0.0       # OpenAI 兼容接口封装
langchain-community>=0.3.0,<1.0.0    # 社区集成（Chroma、文档加载器等）
langchain-text-splitters>=0.3.0,<2.0.0  # 文本分块器
langchain-chroma>=0.1.0,<2.0.0       # Chroma 向量库 LangChain 集成
langchain-huggingface>=0.1.0,<2.0.0  # HuggingFace Embeddings 封装
langchain-core>=0.3.0,<2.0.0         # LangChain 核心
langgraph>=1.0.0,<2.0.0              # StateGraph + checkpointer（深度模式 Agent 编排）
langgraph-checkpoint-sqlite>=2.0.0,<4.0.0  # SQLite checkpointer（Agent 状态持久化，ask_user 跨重启恢复）

# --- 向量数据库 ---
chromadb>=0.5.0,<2.0.0               # Chroma 向量库（持久化）

# --- 文档解析 ---
python-docx>=1.1.0,<2.0.0            # Word .docx 解析

# --- 本地 Embedding 模型 ---
sentence-transformers>=3.0.0,<6.0.0  # 本地向量化（BGE 中文模型）
python-dotenv>=1.0.0,<2.0.0          # .env 环境变量加载
tiktoken>=0.7.0,<1.0.0               # OpenAI token 计数（精确分块）

# --- Web 服务 ---
fastapi>=0.115.0,<1.0.0              # API 框架
uvicorn[standard]>=0.30.0,<1.0.0     # ASGI 服务器
pydantic>=2.0.0,<3.0.0               # 数据校验

# --- 异步 HTTP ---
httpx>=0.27.0,<1.0.0                 # 异步 HTTP 客户端（OA API 调用）

# --- 模型下载（国内备选）---
modelscope>=1.0.0,<2.0.0             # ModelScope SDK（HuggingFace 被墙时下载模型）

# --- 开发和测试 ---
pytest>=8.0.0,<10.0.0                # 测试框架
pytest-asyncio>=0.24.0,<2.0.0        # 异步测试支持
